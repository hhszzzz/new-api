name: Publish fork image

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: publish-fork-image-main
  cancel-in-progress: false

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: Build and publish AMD64 image
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Write image version
        run: echo "main-${GITHUB_SHA::8}" > VERSION

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@c99871dec2022cc055c062a10cc1a1310835ceb4 # v4.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/hhszzzz/new-api:main
            ghcr.io/hhszzzz/new-api:main-${{ github.sha }}
          labels: |
            org.opencontainers.image.source=https://github.com/hhszzzz/new-api
            org.opencontainers.image.revision=${{ github.sha }}
          cache-from: type=gha,scope=fork-main-amd64
          cache-to: type=gha,mode=max,scope=fork-main-amd64
